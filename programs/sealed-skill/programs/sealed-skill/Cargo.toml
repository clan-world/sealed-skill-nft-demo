[package]
name = "sealed-skill"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "sealed_skill"

[features]
default = []

[dependencies]
anchor-lang = { version = "0.32.1", features = ["init-if-needed"] }
